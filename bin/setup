#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install
cd spec/support/rails_app/ && bundle exec rake db:migrate RAILS_ENV=test && cd ../../../

# Do any other automated setup that you need to do here
